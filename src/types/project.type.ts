type ProjectType = {
  name: string;
  description: string;
  tools: string;
  images: {
    web: string;
  };
  url: {
    demo: string;
    github: string;
  };
};

export default ProjectType;
